# Jupyter

How to create a Jupiter kernel and a notebook in vs code exactly to your specs:
FIrst you create a venv

    python3 -m venv .jupenv


the IPython kernel can be installed with any othe packages you prefer using requirements.txt

#### installing Ipython kernel
https://ipython.readthedocs.io/en/latest/install/kernel_install.html


#### install jupyter
jupyter itself should be in pipx?
cause we want the same jupyter to switch between kernels?
https://jupyter.org/install
