---
services:
  book-runner:
    image: tfds/papermill-base:${TFDS_STACK_VERSION}
    container_name: papermill-base
    environment:
      TFDS_CONFIG_URL: ${TFDS_CONFIG_URL}
    networks:
      - tfds-network

networks:
  tfds-network:
    external: true  # Use the existing tfds-network