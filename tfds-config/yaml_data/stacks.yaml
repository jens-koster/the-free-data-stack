doc: The stack definitions for tfds cli. tfds will do some magic and then iterate
  these folders in order to run docker compose with the chosen command in each

config:
  spark-stack:
    plugins:
    - tfds-config
    - s3-minio
    - postgres
    - papermill
    - spark
    - redis
    - airflow
  book-runner:
    plugins:
    - tfds-config
    - s3-minio
    - papermill
  spark-mini:
    plugins:
    - tfds-config
    - s3-minio
    - postgres
    - spark
  airflow-mini:
    plugins:
    - tfds-config
    - s3-minio
    - postgres
    - redis
    - airflow
